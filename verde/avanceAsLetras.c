#include <stdio.h>

int main()
{
    /*
    int n;
    char A[10001], B[10001];
    scanf("%d", &n);
    fflush(stdin);
    int resp[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%s %s", A, B);
        int total = 0;
        for (int j = 0; A[j] != '\0'; j++)
        {
            int troca = (B[j] - A[j] + 26) % 26;
            total += troca;
            fflush(stdin);
        }
        resp[i] = total;
    }
    for (int i = 0; i < n; i++)
        printf("%d\n", resp[i]);
    */
}